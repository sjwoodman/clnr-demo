metadata:
  name: ${project.artifactId}
data:
  project-stages.yml: |-
    swarm:
      datasources:
        data-sources:
          ReadingsDS:
            driver-name: mysql
            connection-url: jdbc:mysql://mysql:3306/reading?autoReconnect=true&useSSL=false
            user-name: mysql
            password: mysql